# inventory.ini - host lifecycle tasks run locally; the running Ward machine is
# reconciled over SSH through its virtual Ethernet connection.
[local]
localhost ansible_connection=local

[ward_machines]
ward ansible_host=ward ansible_user=root
