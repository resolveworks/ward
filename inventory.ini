# inventory.ini - Ward is configured entirely on the local host.
# connection: local avoids SSH; become (set in the playbooks) provides the
# required privilege escalation via sudo instead of embedding it in commands.
[local]
localhost ansible_connection=local
