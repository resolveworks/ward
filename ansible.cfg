# ansible.cfg - project-local Ansible configuration for Ward.
# Run playbooks from the repository root (where this file lives).
[defaults]
inventory = inventory.ini
host_key_checking = False
retry_files_enabled = False
